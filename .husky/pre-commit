echo "🚀 Rodando hook de pre-commit do Husky..."

# Atualiza a versão automaticamente
node ./scripts/bump-version.cjs

# Garante que o package.json e version.json modificados sejam commitados
git add package.json public/version.json

# Lint-staged (se estiver configurado)
npx lint-staged

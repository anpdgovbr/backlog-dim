import type { ControladorDto } from "@anpdgovbr/shared-types"

/**
 * Alias utilizado internamente para representar um Controlador (Requerido).
 *
 * Mantido para compatibilidade com os componentes que já consomem este tipo.
 */
export type RequeridoOutput = ControladorDto
